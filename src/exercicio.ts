function multiplica(a: number, b: number): number {
    return a * b
};

function dizOla(nome: string): string {
    return `Olá, ${nome}`
} 

console.log(multiplica(15, 48));
console.log(dizOla("Vitor"));